# Local read-only DB config for tools/db_readonly.py
# Copy to tools/db_readonly.local.env for machine-local test usage.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=artistsion
DB_USER=your_user
DB_PASSWORD=your_password
